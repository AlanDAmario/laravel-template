import "./bootstrap";

// import  default scss styles
import "~resources/scss/app.scss";

// path for managing img elements
import.meta.glob(["../img/**"]);

// Import all of Bootstrap's JS
import * as bootstrap from 'bootstrap';
